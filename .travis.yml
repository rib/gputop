
language: c

os:
    - linux

compiler:
    - clang
    - gcc

cache:
    directories:
        - emsdk

#    - if ! test -d ./emsdk; then mkdir emsdk; pushd emsdk; wget https://s3.amazonaws.com/mozilla-games/emscripten/releases/emsdk-portable.tar.gz; unzip *.zip; chmod +x ./emsdk; dos2unix ./emsdk; ./emsdk update; popd; fi
before_install:
    - git clone --single-branch --branch master --depth 1 https://github.com/google/protobuf
    - (cd protobuf && ./autogen.sh && ./configure --prefix=$TRAVIS_BUILD_DIR/protobuf-bin && make && sudo make install)

addons:
    apt:
        sources:
            - ubuntu-toolchain-r-test
        packages:
            - gcc-4.8
            - g++-4.8
            - clang
            - autopoint
            - python-lxml

before_script:
    - NOCONFIGURE=1 ./autogen.sh

script:
    - ./configure CC="gcc-4.8" CXX="g++-4.8" && make
